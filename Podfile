# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'curve-challenge' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for curve-challenge
  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'lottie-ios'

  target 'curve-challengeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'curve-challengeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
